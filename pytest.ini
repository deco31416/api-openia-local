[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -v --tb=short
markers =
    slow: tests que requieren mas tiempo
    integration: tests de integracion
    unit: tests unitarios
